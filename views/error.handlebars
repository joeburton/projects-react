    <div class="intro">
        <div class="container">
            <h1>Project Directory</h1>
            <p>There appears to have been an error.</p>
            <p>{{ message }}</p>
            <p>{{ error }}</p>
            <p>{{ status }}</p>
        </div>
    </div>